A function, in my opinion, useful in mathematical calculations is the logarithm in base n.

#include <math.h>
#include "float.h"

double
logn(base, val)
int base;
double val;
{
        return log(val)/log(base);
}
